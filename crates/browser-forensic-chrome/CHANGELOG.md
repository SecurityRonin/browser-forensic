# Changelog

All notable changes to this project will be documented in this file.

The format is based on [Keep a Changelog](https://keepachangelog.com/en/1.0.0/),
and this project adheres to [Semantic Versioning](https://semver.org/spec/v2.0.0.html).

## [Unreleased]

## [0.2.1](https://github.com/SecurityRonin/browser-forensic/compare/browser-forensic-chrome-v0.2.0...browser-forensic-chrome-v0.2.1) - 2026-07-24

### Added

- *(chrome)* route SimpleCache EOF key_size read through safe-read
- *(chrome,cli,triage)* GREEN Extension Cookies parser + wiring
- *(chrome)* GREEN CHIPS partition key on Cookies parser
- *(chrome,cli,triage)* GREEN Media History parser + wiring
- *(chrome,cli,triage)* GREEN Network Action Predictor parser + wiring
- *(chrome,cli,triage)* GREEN Shortcuts parser + wiring
- *(chrome,cli,triage)* GREEN Top Sites parser + wiring
- *(chrome,cli,triage)* GREEN Favicons parser + wiring
- *(core,chrome)* GREEN saturating timestamps + broken_until Unix-seconds fix + tier-1 oracle
- *(chrome,core)* GREEN TransportSecurity hashed-HSTS parser + unix_secs_f64 helper
- *(chrome)* GREEN DIPS/BTM schema-introspecting recovered-domain parser
- *(chrome)* GREEN Reporting and NEL recovered-domain parser
- *(chrome)* GREEN Network Persistent State recovered-domain parser
- *(chrome)* GREEN per-site permission grants parser
- *(chrome)* GREEN Web Data account/payment metadata parser
- *(chrome)* GREEN login_data surfaces signon_realm, times_used, password-age
- *(chrome)* GREEN visit_id, chain_start, client/server redirect_kind
- *(prefs)* GREEN — Chrome Preferences + Firefox prefs.js parsers

### Other

- adopt release-plz — publish only intended-public crates, gate the rest (fleet standard)
- *(chrome)* truncation sweep for SimpleCache EOF-record read
- *(chrome)* document Reading List deferral (Sync Data LevelDB)
- *(chrome)* RED Extension Cookies parser (cookie_store tag)
- *(chrome)* RED CHIPS partition key on Cookies parser
- *(core,chrome)* RED Media History parser (playback/session/origin)
- *(core,chrome)* RED Network Action Predictor parser
- *(core,chrome)* RED Shortcuts parser (omnibox typed text)
- *(core,chrome)* RED Top Sites parser (most-visited/frecency)
- *(core,chrome)* RED Favicons parser (page_url visited-URL source)
- *(chrome)* satisfy clippy in recovered-domains oracle test (is_ok_and, inline format)
- *(core,chrome)* RED broken_until is Unix seconds + timestamp helpers must saturate
- *(chrome,core)* RED TransportSecurity hashed-HSTS parser + unix_secs_f64 helper
- *(chrome)* RED DIPS/BTM schema-introspecting recovered-domain parser
- *(chrome)* RED Reporting and NEL recovered-domain parser (SQLite + legacy JSON)
- *(chrome)* RED Network Persistent State recovered-domain parser
- *(chrome)* RED per-site permission grants from Preferences JSON
- *(chrome)* RED Web Data account/payment metadata (cards, tokens, profiles)
- *(chrome)* RED login_data credential metadata (signon_realm, times_used, ...)
- *(chrome)* RED visit_id, chain_start, client/server redirect_kind
- *(prefs)* RED — Chrome Preferences + Firefox prefs.js parsers
# Changelog

All notable changes to `browser-forensic-chrome` are documented here.
Entries are generated by release-plz from conventional commits.
