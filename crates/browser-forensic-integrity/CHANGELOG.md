# Changelog

All notable changes to this project will be documented in this file.

The format is based on [Keep a Changelog](https://keepachangelog.com/en/1.0.0/),
and this project adheres to [Semantic Versioning](https://semver.org/spec/v2.0.0.html).

## [Unreleased]

## [0.2.1](https://github.com/SecurityRonin/browser-forensic/compare/browser-forensic-integrity-v0.2.0...browser-forensic-integrity-v0.2.1) - 2026-07-24

### Added

- *(integrity)* route SQLite header field reads through safe-read
- *(cli,triage,discovery)* wire WebCache (IE/Edge-Legacy) parsing
- *(carve,integrity)* GREEN — RecoveredDeletedHistory indicator
- *(integrity)* GREEN — check_incognito_residue (residue + absence)
- *(integrity)* GREEN — check_header_anomalies (change counter, versions, sequence)
- *(integrity)* GREEN — bounds-checked SQLite header parser
- *(integrity)* GREEN — TimestampInFuture + LastVisitMismatch
- *(integrity)* GREEN — VisitCountMismatch (visit_count > surviving visits)
- *(integrity)* GREEN — check_page_state (freelist growth + page-count mismatch)
- *(integrity)* GREEN — observation() + innocent_alternative() per finding

### Fixed

- *(lint)* clear pre-existing clippy breaks blocking the workspace gate

### Other

- adopt release-plz — publish only intended-public crates, gate the rest (fleet standard)
- *(integrity)* truncation sweep for the SQLite header parser
- *(cli,integrity)* RED — resumable checkpoint
- *(integrity)* tier-2 controlled-scenario tampering oracle
- *(integrity)* RED — incognito residue-plus-absence indicator
- *(integrity)* RED — manual-DB-edit header indicators
- *(integrity)* RED — bounds-checked SQLite header parser
- *(integrity)* RED — future timestamps + last_visit mismatch
- *(integrity)* RED — visit_count vs surviving visits mismatch
- *(integrity)* RED — freelist-growth / page-state indicator
- *(integrity)* RED — framing methods (observation + innocent_alternative)
# Changelog

All notable changes to `browser-forensic-integrity` are documented here.
Entries are generated by release-plz from conventional commits.
