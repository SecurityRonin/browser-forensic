[package]
name = "snss-core"
version = "0.1.0"
description = "Read-only decoder for Chromium/Brave SNSS session files (the fleet's Chrome session reader)"
edition.workspace = true
rust-version.workspace = true
license.workspace = true

[lib]
name = "snss"
path = "src/lib.rs"

[lints]
workspace = true
