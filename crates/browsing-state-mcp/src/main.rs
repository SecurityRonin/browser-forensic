//! `browsing-state-mcp` server entry point. The stdio JSON-RPC MCP loop is wired
//! once the redaction + tool layers are in place.

fn main() {
    eprintln!("browsing-state-mcp: server loop implemented in a later step");
}
